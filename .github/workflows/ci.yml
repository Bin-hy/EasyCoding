name: CI

on:
  push:
    branches: [main]
    paths:
      - "mewcode/**"
  pull_request:
    branches: [main]
    paths:
      - "mewcode/**"

defaults:
  run:
    working-directory: mewcode

jobs:
  # ───────────── 代码检查 ─────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: mewcode/go.mod

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          working-directory: mewcode

  # ───────────── 模块测试 ─────────────
  test:
    name: Test
    needs: lint
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        go: ["1.25", "1.26"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}

      - name: Run tests
        run: go test -v -race -coverprofile=coverage.out ./...

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.os }}-go${{ matrix.go }}
          path: mewcode/coverage.out

  # ───────────── 构建检查 ─────────────
  build:
    name: Build Check
    needs: test
    strategy:
      matrix:
        target:
          - { os: linux,   arch: amd64 }
          - { os: linux,   arch: arm64 }
          - { os: darwin,  arch: amd64 }
          - { os: darwin,  arch: arm64 }
          - { os: windows, arch: amd64 }
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: mewcode/go.mod

      - name: Build
        env:
          CGO_ENABLED: 0
          GOOS: ${{ matrix.target.os }}
          GOARCH: ${{ matrix.target.arch }}
        run: |
          cd mewcode
          mkdir -p dist
          out="dist/mewcode-$GOOS-$GOARCH"
          [ "$GOOS" = "windows" ] && out="${out}.exe"
          go build -ldflags="-s -w" -o "$out" ./cmd/mewcode/
